---
import styles from './buttonComponent.module.scss'

interface Props {
  href?: string
  type?: 'button'
  class?: string
}

const { href = '#', type = 'button', class: buttonClass } = Astro.props
---

<a href={href} class:list={[styles.button, buttonClass]} type={type}><slot /></a
>
