---
import styles from './contentWrapper.module.scss'

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class:list={[styles.contentWrapper, className]}>
  <slot />
</div>
